<div class="max-w-6xl mx-auto">
  <div class="flex justify-between items-center mb-8">
    <h1 class="text-3xl font-bold text-gray-800">Meus Clubes de Leitura</h1>
    <%= link_to "Criar Novo Clube", new_club_path, class: "bg-blue-600 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-lg" %>
  </div>

  <% if @clubs.empty? %>
    <div class="bg-white p-8 rounded-lg shadow-md text-center">
      <div class="text-gray-500 mb-4">
        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-16 h-16 mx-auto">
          <path stroke-linecap="round" stroke-linejoin="round" d="M12 6.042A8.967 8.967 0 0 0 6 3.75c-1.052 0-2.062.18-3 .512v14.25A8.987 8.987 0 0 1 6 18c2.305 0 4.408.867 6 2.292m0-14.25a8.966 8.966 0 0 1 6-2.292c1.052 0 2.062.18 3 .512v14.25A8.987 8.987 0 0 0 18 18a8.967 8.967 0 0 0-6 2.292m0-14.25v14.25" />
        </svg>
      </div>
      <h2 class="text-2xl font-semibold mb-4">Você ainda não participa de nenhum clube</h2>
      <p class="text-gray-600 mb-6">Crie seu primeiro clube ou peça para ser adicionado em um clube existente.</p>
      <%= link_to "Criar Meu Primeiro Clube", new_club_path, class: "bg-blue-600 hover:bg-blue-700 text-white font-bold py-3 px-6 rounded-lg" %>
    </div>
  <% else %>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      <% @clubs.each do |club| %>
        <div class="bg-white rounded-lg shadow-md overflow-hidden hover:shadow-lg transition-shadow duration-300">
          <div class="bg-gradient-to-r from-blue-500 to-blue-700 h-24 flex items-center justify-center">
            <h2 class="text-2xl font-bold text-white px-4 text-center"><%= club.name %></h2>
          </div>
          <div class="p-6">
            <p class="text-gray-600 mb-4 h-20 overflow-hidden"><%= club.description %></p>
            <div class="flex justify-between items-center">
              <% admin = club.club_users.find_by(user: current_user)&.is_admin %>
              <% if admin %>
                <span class="bg-yellow-100 text-yellow-800 text-xs font-medium px-2.5 py-0.5 rounded">Administrador</span>
              <% else %>
                <span class="bg-blue-100 text-blue-800 text-xs font-medium px-2.5 py-0.5 rounded">Membro</span>
              <% end %>
              <%= link_to "Acessar", club_path(club), class: "bg-blue-600 hover:bg-blue-700 text-white text-sm font-medium py-1 px-3 rounded" %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  <% end %>
</div> 